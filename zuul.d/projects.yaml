---
- project:
    name: master-caster/zuul-config
    default-branch: main
    merge-mode: "merge"
    check:
      jobs:
        - noop
    gate:
      jobs:
        - noop
    post:
      jobs:
        - noop
    tag:
      jobs:
        - noop
