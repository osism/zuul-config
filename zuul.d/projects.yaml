---
- project:
    name: SovereignCloudStack/zuul-config
    default-branch: main
    merge-mode: "squash-merge"
    compliance_check:
      jobs:
        - openstack-scs-flavor-check
    check:
      jobs:
        - noop
    gate:
      jobs:
        - noop
    post:
      jobs:
        - noop
    tag:
      jobs:
        - noop
