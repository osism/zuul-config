---
- name: Base pre
  hosts: all
  roles:
    - emit-job-header
    - log-inventory
    - add-build-sshkey
    - start-zuul-console
    - validate-host
    - prepare-workspace-log
    - ensure-output-dirs-present
    - diagnose-first-sudo
