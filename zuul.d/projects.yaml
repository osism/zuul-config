---
- project:
    name: osism/zuul-config
    default-branch: main
    check:
      jobs:
        - ansible-lint
    gate:
      jobs:
        - ansible-lint
    post:
      jobs:
        - noop
    tag:
      jobs:
        - noop
